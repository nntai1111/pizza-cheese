import { Injectable } from '@angular/core';

@Injectable({ providedIn: 'root' })
export class WebsocketService {
  connect(): void {
    // SockJS + STOMP integration placeholder
  }

  disconnect(): void {
    // SockJS + STOMP integration placeholder
  }
}
